import FileUploadArea from '../../components/Experimental/FileUpload/FileUploadArea';
import {Meta, Preview, Story} from '@storybook/addon-docs/blocks';
import {STORY_CATEGORIES} from '../storybookUtils';

<Meta title={`${STORY_CATEGORIES.EXPERIMENTAL}/FileUploadArea`} />

# FileUploadArea

```js
import FileUploadArea from '@fbcsymphony/design-system/components/Experimental/FileUpload/FileUploadArea';
```

<Preview>
  <Story name="FileUploadArea">
    <FileUploadArea onClick={() => alert('Choose a file')} />
  </Story>
</Preview>
